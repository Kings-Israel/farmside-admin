let getters = {
    status: state => state.status,
    aboutMainSection: state => state.aboutMainSection,
    aboutSubSection: state => state.aboutSubSection,
    user: state => state.user
}

export default getters